# Sổ phát hành Thansa OS (neo với Javis)

Thansa OS đánh version RIÊNG (semver: `MAJOR.MINOR.PATCH`), độc lập với số của Javis OS
(nền upstream). Mỗi bản Thansa vẫn dựng trên một bản Javis cụ thể — đó là **điểm neo**.

- **Nguồn sự thật của neo:** file `VERSION` mang dạng `<semver-thansa>-javis-<nền-javis>`
  (vd `1.2.0-javis-0.40.0`). Phần `-javis-...` là NEO nội bộ, **ẩn khỏi mọi chỗ người dùng
  thấy** (UI chỉ hiện `v1.2.0`, tag ảnh chỉ là `1.2.0`). `ops/moc-goc.json` giữ neo chính
  thức (`thansa_version` + `goc_version` + `goc_commit`).
- **So bản mới** dùng semver Thansa (hàm `ver_tuple` tự bỏ đuôi neo). **So "đã cài" trên trang
  Nhật ký** dùng phần nền Javis (changelog đánh số theo Javis).
- Mỗi lần phát hành: thêm một dòng vào bảng dưới, và cập nhật `VERSION` + `moc-goc.json` khớp nhau.

| Thansa | Nền Javis | Commit gốc | Ngày | Ghi chú |
|--------|-----------|------------|------|---------|
| 1.10.0 | 0.63.0    | `5159f5a`  | 2026-09-22 (đã phát hành, origin/main 7b0b94c) | Trộn Javis 0.62.5→0.63.0 (2 commit): **Mục Coding** - mở phiên chat chạy thẳng trong một repo git (gắn nhánh, worktree riêng, điểm hồi để quay lại), tách khỏi khung chat chung. **Tablet xoay ngang bung hết màn** (bỏ khoá chiều dọc PWA). Giữ nguyên mọi tuỳ biến: chốt an toàn, xưng hô bạn/mình, nhãn "Thansa", dịch EN Store+usage, nhật ký song ngữ. |
| 1.9.0  | 0.62.5    | `f1a5891`  | 2026-09-22 (đã phát hành, origin/main 84ab334) | Trộn Javis 0.60.1→0.62.5 (15 commit): **Gộp Chatbot vào trang Hội thoại** - sổ đăng ký kênh, tài khoản bot thuộc về một brain, trả lời khách ngay từ app; form tạo bot đi hai bước; tab Kênh đổi tên **Tài khoản bot**; chatbot chạy đúng model của trợ lý. **Trang Mức dùng hết lag** (đọc tiếp transcript thay vì parse lại, vẽ trước khi quét, phân trang phía máy chủ). **Trợ lý có tủ tài liệu & link riêng**. Trang Nhật ký cập nhật nhanh hơn (cache máy chủ, trả theo trang). App HTML chia sẻ hết trắng trang. Giữ nguyên: chốt an toàn, xưng hô bạn/mình, nhãn "Thansa", dịch EN Store + usage. |
| 1.8.0  | 0.60.1    | `423a83e`  | 2026-09-21 (trộn xong, cuộn vào 1.9.0 - KHÔNG phát hành riêng) | Trộn Javis 0.55.63→0.60.1 (93 commit): **Ra lệnh & trò chuyện bằng giọng nói** - Voice điều khiển dashboard và app máy tính bằng lời; bộ não giọng riêng nghe bằng Groq, chữ hiện theo lời như ChatGPT Voice, ngắt lời được, "nói" và "làm" chạy tách luồng. **Linh vật** nép ở mép màn hình + logo mới, thanh bên gọn một tầng. **Trang Cộng sự**: chat thẳng với từng trợ lý hoặc quy trình, xem lại lịch sử chạy, gõ lệnh gạch chéo. **Hộp thư Hội thoại**: gom tin khách nhắn bot Telegram/Zalo và Zalo cá nhân về một chỗ để đọc lại và tiếp quản; thêm tầng **CRM** (khách hàng, thẻ, ghi chú) qua gói Kho. **Thansa vẫn GIỮ chốt an toàn**: khác upstream, loop/việc mặc định "chỉ đọc + đề xuất", KHÔNG tự tiêu tiền/lên đơn/đăng bài/nhắn khách trừ khi bạn ra lệnh rõ. |
| 1.7.0  | 0.55.63   | `9ddc3d0`  | 2026-09-09 (trộn xong, cuộn vào 1.8.0 - KHÔNG phát hành riêng) | Trộn Javis 0.55.53→0.55.63 (10 commit): **ảnh/file đính kèm không rơi mất khi Enter sớm**, link file:// bấm mở được, tạo file từ chat trong thư mục brain, vá 3 lỗi 08/09 (503 Antigravity, kết quả việc ngầm bị cắt, xem file trên điện thoại), Watchtower tự cập nhật, Antigravity bỏ --effort thừa, **giao diện dịch nốt sang tiếng Anh** (1245 chuỗi). **Thansa GIỮ chốt an toàn**: khác upstream, loop/việc tạo từ chat vẫn mặc định "chỉ đọc + đề xuất", KHÔNG tự tiêu tiền/lên đơn/đăng bài/nhắn khách trừ khi bạn ra lệnh rõ (upstream 0.55.62 đã bỏ luật này, Thansa giữ lại cho an toàn). |
| 1.7.0  | 0.55.53   | `8a38217`  | (cuộn lên 0.55.63, không phát hành riêng) | Trộn Javis 0.55.46→0.55.53 (7 commit, vá lỗi + bảo mật): **gắn file & link vào một cuộc trò chuyện**, ô tìm trong Store gõ liền mạch, hai brain chép chung file bộ nhớ không hỏng chỉ mục, vá 3 lỗi 06/09 (wiki trùng trang, app load chậm, ký ức lạc thư mục), thực đơn tool gọi tên từng plugin. **Vá CVE path traversal** (python-multipart 0.0.18→0.0.30). Bảo mật nội bộ: chặn `ops/.telegram` lọt lại vào git. |
| 1.6.0  | 0.55.46   | `886ac7e`  | 2026-09-05 (đã phát hành) | Trộn Javis 0.52.10→0.55.46 (57 commit): **Kho gói Thansa Store** ngay trong app - duyệt danh mục trợ lý/kỹ năng/quy trình/connector rồi bấm Cài (gói không bao giờ đè thứ bạn tự viết); **độ sâu suy nghĩ** có tác dụng thật trên Codex/Grok/Antigravity; **tài liệu song ngữ Việt-Anh**; gom nhóm Agent/Workflow; Fable 5.1 trong danh sách Claude Code; Ollama Local; vá mic lặp/đính kèm file/đăng nhập Antigravity dán mã. "Người gác cổng bản cũ" cảnh báo khi trình duyệt chạy code cũ. Rebrand toàn UI + Store sang Thansa. Kho gói mặc định vẫn lấy từ catalog upstream (đổi được trong Cài đặt). |
| 1.5.0  | 0.52.10   | `91aa663`  | 2026-09-01 (đã phát hành) | Trộn Javis 0.50.2→0.52.10: **Grok chạy mượt** (hết trả ô trống, hết đăng xuất mỗi lần cập nhật), **ảnh gửi vào chat ở lại + bấm phóng to**, cập nhật xong không rớt MCP, nguồn POS không rớt đơn thứ 2, Antigravity CLI treo không kéo sập dashboard, chat dài tiếng Việt hết nổ "Argument list too long", ô tìm note nhanh hơn. Upstream tự thêm **giao diện tiếng Anh gốc** (màn chính + Models/Việc/Studio + ô đổi ngôn ngữ) - Thansa neo lớp dịch + overlay lên trên, đổi ngôn ngữ tức thì không cần tải lại. Rebrand chuyển xuống tầng từ điển i18n (vi.json/en.json). |
| 1.4.0  | 0.50.2    | `60830fa`  | 2026-08-29 (đã phát hành) | Trộn Javis 0.47→0.50.2: **bộ não Grok Build** (gói SuperGrok/X Premium+) thay Gemini CLI (Google đã ngắt hạng cá nhân), **hòm thư + thông báo đẩy trình duyệt**, ChatGPT tạo ảnh, đấu Hostinger, agent chọn đúng model mọi nhà cung cấp, Telegram giữ ngữ cảnh qua restart. System prompt sang tiếng Anh (tiết kiệm ~2.000 token/lượt, vẫn nói tiếng Việt với user). Rebrand chuỗi hiển thị mới. Còn 248 chuỗi UI chờ dịch EN (suy biến về tiếng Việt). |
| 1.3.0  | 0.47.2    | `78dff14`  | 2026-08-27 (đã phát hành) | Trộn Javis 0.43→0.47.2 (tự-học ra Agent/Workflow không đè, sync cả ảnh, banner Model + PWA cài desktop, trần tool 8→30 + phanh kẹt-vòng, gọn Vault). Dịch UI mới; vá test timing (dứt điểm ws-disconnect) + trần prompt. Phát hành qua **snapshot main** (ff cho user cũ). |
| 1.2.0  | 0.43.2    | `fac4746`  | 2026-08-25 (đã phát hành) | Trộn Javis 0.40→0.43.2 (YouTube phụ đề, đổi model giữ mạch, Antigravity MCP hub, Telegram sidebar); changelog song ngữ; dịch plugin/chatbot/UI mới; version Thansa riêng + neo; rebrand bản quyền/link/tác giả → Duy Quang. Đẩy `main`, image `:1.2.0`. |
| 1.1    | 0.35.10   | (nhánh `release`) | 2026-08 | Bản Thansa OS đầu tiên phát hành (rebrand + EN). |
